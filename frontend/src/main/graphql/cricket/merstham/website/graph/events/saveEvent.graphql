mutation saveEvent ($event: EventInput!) {
    saveEvent(event: $event) {
        id
        eventDate
        title
        body
        path
        uuid
        location
        callToActionLink
        callToActionDescription
        banner
        attributes {
            key
            value
        }
    }
}

mutation createMember($member: MemberInput!) {
  createMember(data: $member) {
    id
    registrationDate
    attributes {
      definition {
        key
      }
      value
    }
    subscription {
      price
      priceListItem {
        id
      }
      year
    }
  }
}

mutation createMemberSubscription($member: MemberInput!) {
  createMemberSubscription(data: $member) {
    id
    registrationDate
    attributes {
      definition {
        key
      }
      value
    }
    subscription {
      price
      priceListItem {
        id
      }
      year
    }
  }
}

query feed($page: Int!) {
    eventsFeed(page: $page) {
        id
        eventDate
        title
        path
        uuid
        location
        body
        callToActionLink
        callToActionDescription
        banner
        attributes {
            key
            value
        }
    }

    eventTotals {
        totalRecords
    }
}

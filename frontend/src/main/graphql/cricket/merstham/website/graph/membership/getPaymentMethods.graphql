query getPaymentMethods($owner: String!) {
    getPaymentMethods(userId: $owner) {
        id
        userId
        createDate
        provider
        type
        customerIdentifier
        methodIdentifier
        status
    }
}
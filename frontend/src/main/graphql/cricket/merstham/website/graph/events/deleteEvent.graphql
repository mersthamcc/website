mutation deleteEvent($id: Int!) {
    deleteEvent(id: $id) {
        id
        eventDate
        title
        body
        path
        uuid
        location
        attributes {
            key
            value
        }
    }
}

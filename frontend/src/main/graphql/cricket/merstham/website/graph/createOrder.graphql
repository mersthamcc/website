mutation createOrder ($uuid: String!, $total: Float!, $discount: Float!) {
    createOrder(uuid: $uuid, total: $total, discount: $discount) {
        id
        uuid
        ownerUserId
        accountingId
        createDate
        total
        discount
    }
}
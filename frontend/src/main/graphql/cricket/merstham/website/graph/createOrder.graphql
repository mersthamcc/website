mutation createOrder ($uuid: String!, $total: Float!, $discount: Float!, $coupons: [String]) {
    createOrder(uuid: $uuid, total: $total, discount: $discount, coupons: $coupons) {
        id
        uuid
        ownerUserId
        accountingId
        createDate
        total
        discount
    }
}
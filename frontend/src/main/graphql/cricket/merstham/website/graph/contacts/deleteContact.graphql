mutation deleteContact($id: Int!) {
    deleteContact(id: $id) {
        id
        name
        position
        slug
        category {
            id
            title
            slug
        }
        methods {
            key
            value
        }
    }
}

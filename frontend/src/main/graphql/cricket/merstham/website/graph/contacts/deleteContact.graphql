mutation deleteContact($id: Int!) {
    deleteContact(id: $id) {
        id
        name
        position
        slug
        sortOrder
        category {
            id
            title
            slug
            sortOrder
        }
        methods {
            key
            value
        }
    }
}

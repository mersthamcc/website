query getNewsItem($id: Int!) {
    newsItem(id: $id) {
        id
        uuid
        draft
        title
        path
        author
        createdDate
        publishDate
        socialSummary
        featureImageUrl
        body
        attributes {
            key
            value
        }
        publishToFacebook
        publishToTwitter
        publishToInstagram
    }
}
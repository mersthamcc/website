query getNewsItem($id: Int!) {
    newsItem(id: $id) {
        id
        uuid
        draft
        title
        path
        author
        createdDate
        publishDate
        socialSummary
        body
        attributes {
            key
            value
        }
        publishToFacebook
        publishToTwitter
    }
}
query getNewsItem($id: Int!) {
    newsItem(id: $id) {
        id
        uuid
        draft
        title
        path
        author
        createdDate
        publishDate
        body
        attributes {
            name
            value
        }
    }
}
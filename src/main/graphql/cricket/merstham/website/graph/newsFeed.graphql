query newsFeed ($page: Int!){
    feed (page: $page){
        id
        title
        author
        path
        body
        createdDate
        publishDate
    }

    newsTotals {
        totalRecords
    }
}
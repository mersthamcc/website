query getNewsItemByPath($path: String!) {
    newsItemByPath(path: $path) {
        id
        uuid
        draft
        title
        path
        author
        createdDate
        publishDate
        body
        attributes {
            name
            value
        }
    }
}
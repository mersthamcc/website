query getNewsItemByPath($path: String!) {
    newsItemByPath(path: $path) {
        id
        title
        path
        author
        createdDate
        publishDate
        body
        attributes {
            name
            value
        }
    }
}
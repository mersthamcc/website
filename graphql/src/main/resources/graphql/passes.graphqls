type Pass {
    type: String!
    serialNumber: String!
    content: String!
}

extend type Query {
    pass(memberUuid: String!, type: String!): Pass!
}

extend type Mutation {
    distributePasses(memberId: Int!): [String!]
}
mutation($member: MemberInput!) {
  createMember(data: $member) {
    id
    givenName
    familyName
    registrationDate
    attributes {
      definition {
        key
      }
      value
    }
    owner {
      email
    }
  }
}

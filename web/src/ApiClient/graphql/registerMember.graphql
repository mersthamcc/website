mutation($member: MemberInput!) {
  createMember(data: $member) {
    id
    registrationDate
    attributes {
      definition {
        key
      }
      value
    }
    owner {
      email
    }
  }
}
